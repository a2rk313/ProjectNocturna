import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Project Nocturna - AI-Optimized Dark Sky Explorer',
  description: 'A comprehensive WebGIS platform for discovering dark sky sites and analyzing light pollution trends using NASA datasets.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}