# Development environment configuration
# Use this for local development outside Docker

# Database Configuration - Local Development
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=nocturna
# Use localhost for local development (not 'db' which is Docker service name)
DB_HOST=localhost
DB_PORT=5433

# GeoServer Configuration
GEOSERVER_ADMIN_USER=admin
GEOSERVER_ADMIN_PASSWORD=your_geoserver_password_here

# Application Settings
NODE_ENV=development
PORT=3000
# For local development, your frontend runs on a different port
ALLOWED_ORIGIN=http://localhost:3000
# Secret key to authorize database seeding
ADMIN_SECRET=super_secret_admin_key